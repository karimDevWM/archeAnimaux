{% extends 'base.html.twig' %}

{% block title %}Refuges index{% endblock %}

{% block body %}
    <h1>Refuges index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Mail</th>
                <th>Adresse</th>
                <th>Cp</th>
                <th>Ville</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for refuge in refuges %}
            <tr>
                <td>{{ refuge.id }}</td>
                <td>{{ refuge.nom }}</td>
                <td>{{ refuge.mail }}</td>
                <td>{{ refuge.adresse }}</td>
                <td>{{ refuge.cp }}</td>
                <td>{{ refuge.ville }}</td>
                <td>
                    <a href="{{ path('app_refuges_show', {'id': refuge.id}) }}">show</a>
                    <a href="{{ path('app_refuges_edit', {'id': refuge.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_refuges_new') }}">Create new</a>
{% endblock %}
