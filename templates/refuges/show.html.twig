{% extends 'base.html.twig' %}

{% block title %}Refuges{% endblock %}

{% block body %}
    <h1>Refuges</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ refuge.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ refuge.nom }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ refuge.mail }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ refuge.adresse }}</td>
            </tr>
            <tr>
                <th>Cp</th>
                <td>{{ refuge.cp }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ refuge.ville }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_refuges_index') }}">back to list</a>

    <a href="{{ path('app_refuges_edit', {'id': refuge.id}) }}">edit</a>

    {{ include('refuges/_delete_form.html.twig') }}
{% endblock %}
